# 🛠️ Frontend Toolkit

## 🚀 Быстрый доступ

- [🔗 JavaScript плагины](/javascript/)
- [🎨 SCSS миксины](/scss/)
- [📝 Pug шаблоны](/pug/)
- [👀 Демо-примеры](https://larenkosha.github.io/frontend-toolkit/docs/)
- [🔄 Последние обновления](#обновления)

## ⭐ Популярные сниппеты

- [Аккордеон](/javascript/ui-components/accordion.js) `#ui` `#jquery`
- [Якорная навигация](/javascript/navigation/anchor-nav.js) `#nav` `#jquery`
- [Адаптивные миксины](/scss/mixins/_responsive.scss) `#scss` `#responsive`
- [Форма с валидацией](/javascript/forms/form-validation.js) `#form` `#validation`

---

## 🔍 Поиск по тегам

| Категория         | Теги                                         |
| ----------------- | -------------------------------------------- |
| **Навигация**     | `#nav`, `#navigation`, `#scroll`             |
| **UI Компоненты** | `#ui`, `#components`, `#accordion`, `#modal` |
| **Формы**         | `#form`, `#validation`, `#inputs`            |
| **Технологии**    | `#jquery`, `#vanilla`, `#scss`, `#pug`       |
| **Адаптивность**  | `#responsive`, `#mobile`                     |
| **Анимации**      | `#animation`, `#transitions`                 |

---

## 📁 Структура репозитория

### 🎯 JavaScript

- **Навигация** - `#nav`

  - [`anchor-nav.js`](/javascript/navigation/anchor-nav.js) `#nav` `#jquery` `#smooth-scroll`
    - Плавная навигация по якорям с автоподсветкой
  - [`sticky-nav.js`](/javascript/navigation/sticky-nav.js) `#nav` `#jquery` `#scroll`
    - Липкое меню при скролле

- **UI Компоненты** - `#ui`

  - [`accordion.js`](/javascript/ui-components/accordion.js) `#ui` `#jquery` `#accordion`
    - Аккордеон с одиночным/множественным открытием
  - _[будут добавляться...]_

- **Формы** - `#form`
  - _[будут добавляться...]_

### 🎨 SCSS

- **Миксины** - `#scss` `#mixins`
  - [`_responsive.scss`](/scss/mixins/_responsive.scss) `#scss` `#responsive`
    - Медиа-запросы и адаптивные помощники
  - _[будут добавляться...]_

### 📝 Pug

- **Шаблоны** - `#pug`
  - _[будут добавляться...]_

### 👀 Демо

- **Примеры использования** - `#demo`
  - [`anchor-nav-demo.html`](/docs/navigation/anchor-nav-demo.html)
  - _[будут добавляться...]_

## 🏷️ Управление версиями

### 📋 Текущие версии

| Версия   | Описание                      | Дата       |
| -------- | ----------------------------- | ---------- |
| `v1.0.0` | Базовая структура репозитория | 08.11.2025 |

### 💻 Работа с тегами Git

**Добавить тег к последнему коммиту**

```bash
git tag -a v1.0.0 -m "Базовая структура репозитория"
```

**Добавить тег к конкретному коммиту**

```bash
git tag -a v1.1.0 [commit-hash] -m "Добавлены навигационные плагины"
```

**Добавить несколько тегов подряд**

```bash
git tag -a v1.0.0 -m "Version 1.0.0"
git tag -a v1.1.0 -m "Version 1.1.0"
git tag -a v1.2.0 -m "Version 1.2.0"
```

**Отправить теги на GitHub**

```bash
git push origin --tags
```

**Или конкретный тег**

```bash
git push origin v1.1.0
```

**Список всех тегов**

```bash
git tag -l
```

**Подробная информация о теге**

```bash
git show v1.0.0
```

### ❌ Чего избегать:

**НЕ создавать пустые коммиты для версий!**

```bash
git commit --allow-empty -m "Version 1.1.0"
git tag v1.1.0
```

### ✅ Правильный workflow:

**Делаем изменения и коммитим**

```bash
git add .
git commit -m "Добавлен anchor-nav плагин"
```

**Добавляем тег версии**

```bash
git tag -a v1.1.0 -m "Навигационные плагины"
```

**Пушим всё вместе**

```bash
git push origin main --tags
```

## 📝 Как использовать

### Для JavaScript плагинов:

- Подключи jQuery (если требуется)
- Добавь файл плагина
- Инициализируй в своём коде:

```js
$(".element").pluginName({
  option: "value",
});
```

### Для SCSS миксинов:

```scss
@import "path/to/mixins";

.element {
  @include mixin-name(parameter);
}
```

---

## 🔄 Обновления {#обновления}

### v1.0.0 (текущая)

- ✅ Базовая структура репозитория
